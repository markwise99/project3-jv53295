CC=gcc
CFLAGS=-I.

test_user: buffer_user.o test_user.o
	$(CC) -o test_user buffer_user.o test_user.o

run_user:
	./test_user

val_user:
	valgrind ./test

test: buffer.o test.o
	$(CC) -o test buffer.o test.o

run:
	./test

val:
	valgrind ./test

clean:
	rm *.o test test_user
